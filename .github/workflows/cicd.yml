name: CI with k3d - BranchPickingSystem

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    # -------------------------
    # CHECKOUT
    # -------------------------
    - name: Checkout repository
      uses: actions/checkout@v4

    
    #--------------------------
    # BUILD MAVEN
    #--------------------------
    -name: maven
     run : build mvn all modules