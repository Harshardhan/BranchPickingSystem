name: CI-CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKER_HUB_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
  KUBECONFIG_CONTENT: ${{ secrets.KUBECONFIG_CONTENT }}
  PROJECT_NAME: branchpickingsystem
  DOCKER_REGISTRY: docker.io

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: temurin

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Set up Kubeconfig
        run: |
          echo "${KUBECONFIG_CONTENT}" > $HOME/.kube/config

      - name: Build and Push Docker Images
        run: |
          services=("EurekaServerApp" "ConfigServerApp" "API-Gateway" "OrderServiceApplication" "AnalyticsService" "PaymentApplication" "ProductServiceApplication" "NotificationService" "UserRegistration" "ConsolidationService")
          for service in "${services[@]}"; do
            echo "Building Docker image for $service"
            docker build -t $DOCKER_REGISTRY/$DOCKER_HUB_USERNAME/${PROJECT_NAME,,}-$service:$GITHUB_SHA ./$service
            docker push $DOCKER_REGISTRY/$DOCKERHUB_USERNAME/${PROJECT_NAME,,}-$service:$GITHUB_SHA
          done

      - name: Deploy to k3d Kubernetes
        run: |
          kubectl apply -f k8s/
          # Patch deployments with correct image tag
          for service in "${services[@]}"; do
            kubectl set image deployment/$service $service=$DOCKER_REGISTRY/$DOCKER_HUB_USERNAME/${PROJECT_NAME,,}-$service:$GITHUB_SHA --record
          done
          
      - name: Verify Deployments
        run: kubectl get pods -o wide
