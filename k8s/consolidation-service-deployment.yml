apiVersion: apps/v1
kind: Deployment
metadata:
  name: consolidation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consolidation-service
  template:
    metadata:
      labels:
        app: consolidation-service
    spec:
      containers:
        - name: consolidation-service
          image: harsha79/consolidation-service:ci
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
