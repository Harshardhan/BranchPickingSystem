apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eureka-server
      color: blue
  template:
    metadata:
      labels:
        app: eureka-server
        color: blue
        
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: eureka-server
          image: harsha79/eureka-server:PLACEHOLDER
          imagePullPolicy: Always
          ports:
            - containerPort: 8761
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
